{
  "name": "servicenow-external-portal-server",
  "version": "1.0.0",
  "description": "Server-side proxy for ServiceNow External Portal to handle CORS and API authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required'",
    "test": "echo 'No tests specified'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2",
    "helmet": "^7.1.0",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": [
    "servicenow",
    "proxy",
    "cors",
    "api",
    "express"
  ],
  "author": "Your Name",
  "license": "MIT"
}